# =============================================================================
# MyVineOS – Python dependencies
# =============================================================================
# Install (recommended):
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# Notes:
# - Only third-party packages belong here (not stdlib modules).
# - Pins match a known-good local environment; bump intentionally.
# - Runtime: Python 3.10+ recommended (developed on 3.12).
# - Database: MariaDB/MySQL via PyMySQL (see docker-compose.yml for local dev).
# - Local package `poweredbytop/` is vendored in-repo (not on PyPI).

# ─── Web framework ───────────────────────────────────────────────────────────
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
blinker==1.9.0
click==8.4.1

# ─── Config / environment ────────────────────────────────────────────────────
python-dotenv==1.2.2

# ─── Database ────────────────────────────────────────────────────────────────
PyMySQL==1.2.0

# ─── Security / crypto / 2FA ─────────────────────────────────────────────────
cryptography==48.0.0
cffi==2.0.0
pycparser==3.0
pyotp==2.10.0

# ─── HTTP client ─────────────────────────────────────────────────────────────
requests==2.34.2
urllib3==2.7.0
certifi==2026.5.20
charset-normalizer==3.4.7
idna==3.18

# ─── HTML sanitization (rich text / comments) ────────────────────────────────
bleach==6.4.0

# ─── Documents / exports ─────────────────────────────────────────────────────
python-docx==1.2.0
lxml==6.1.1
PyPDF2==3.0.1
typing_extensions==4.15.0
